# Muse 严苛审核规范 v2.0

> 本规范是 soul.md 的落地执行手册。每一次审核都按此流程执行，不允许跳过任何阶段。

---

## 审核全流程总览

```
Phase 0 ── 规则确立 ──→ Phase 1 ── 审核方案 ──→ Phase 2 ── 验证设计 ──→ Phase 3 ── 执行验证 ──→ Phase 4 ── 报告与自省
```

每个 Phase **必须完整执行**，不得跳过。Phase 1 未通过不能进入 Phase 2，依此类推。

---

# Phase 0：规则确立（预审必做）

> **目的：** 在执行任何审核之前，先明确"满分长什么样"。

## 0.1 定义审核范围

回答以下问题并写入审核日志：

| 问题 | 答案格式 |
|:-----|:---------|
| 本次审核什么功能/模块？ | `范围：[描述]` |
| 本次不审核什么？ | `排除：[描述]` |
| 审核的触发条件？ | `触发：[新功能/回归/重构]` |
| 期望的最低通过分？ | `目标分：[0-10]` |

## 0.2 定义各维度满分标准

**在执行审核前**，先明确每个维度的"100分"长什么样。以下是默认满分基准，可根据审核范围调整：

### 功能完整性（权重 30%）

| 满分条件 | 满分=10 |
|:---------|:--------|
| 所有功能按规格完整实现 | ✅ 零遗漏 |
| 所有边界情况已处理 | ✅ 空/满/异常/中断/超长 |
| 所有 P0 问题 | ✅ 零存在 |
| 所有 P1 问题 | ✅ 零存在 |
| P2 问题 ≤ 1 项 | ✅ |

### UI/UX 品质（权重 25%）

| 满分条件 | 满分=10 |
|:---------|:--------|
| 符合 front.md 全部设计规范 | ✅ 逐项对齐 |
| 布局一致性（边距/对齐/间距） | ✅ 像素级统一 |
| 交互反馈完整（涟漪/加载/空态/错误态） | ✅ 四态齐全 |
| 色彩系统正确（MD3 亮/暗） | ✅ 双主题完整 |
| 排版清晰（字号层次/对比度/溢出处理） | ✅ |

### 性能稳定性（权重 20%）

| 满分条件 | 满分=10 |
|:---------|:--------|
| 页面切换无卡顿 | ✅ ≤16ms 帧率 |
| 列表滚动流畅 | ✅ 60fps |
| 网络请求有超时处理 | ✅ ≤10s 超时 |
| 无内存泄漏 | ✅ |
| 后台播放稳定 | ✅ |

### 代码质量（权重 15%）

| 满分条件 | 满分=10 |
|:---------|:--------|
| 架构清晰（MVVM/分层） | ✅ |
| 异常处理完整 | ✅ 无静默 catch，无裸 Throwable |
| 日志分级 | ✅ Error/Warning/Info/Debug |
| 命名规范 | ✅ 驼峰/有意义 |
| 无硬编码 | ✅ 使用资源/常量 |

### 测试覆盖（权重 10%）

| 满分条件 | 满分=10 |
|:---------|:--------|
| 核心路径全链路覆盖 | ✅ |
| 边界情况 | ✅ 空列表/极长名称/特殊字符 |
| 不同数据状态 | ✅ 0条/1条/多条 |
| 异常场景 | ✅ 网络异常/权限拒绝 |

## 0.3 定义评分规则

```
总分 = 功能完整性 × 0.30 + UI/UX × 0.25 + 性能 × 0.20 + 代码 × 0.15 + 测试 × 0.10

评分等级：
  9.5 - 10.0 = 卓越（可通过）
  9.0 - 9.49 = 良好（需修复P1问题后重审）
  8.0 - 8.99 = 及格（需修复P0+P1问题后重审）
  < 8.0      = 不合格（系统性问题，全面返工）

一票否决：任何 P0 问题存在 → 总分自动 ≤ 5.0
```

---

# Phase 1：审核方案（战略层）

> **目的：** 决定"审什么"和"用什么方法审"。这是审核的顶层设计。

## 1.1 方案模板

每次审核前，以此为模板填写：

```markdown
## 审核方案：[主题]

### 审核范围
- 模块/页面：...
- 涉及文件：...
- 测试设备：...

### 维度重点
- 功能：重点/一般/略过（权重调整）
- UI：重点/一般/略过
- 性能：重点/一般/略过
- 代码：重点/一般/略过
- 测试：重点/一般/略过

### 已知风险项
- [风险1]：...
- [风险2]：...

### 方法总览
- 真机/模拟器安装：...
- 自动化测试：...
- 代码审查：...
- 视觉分析/截图：...

### 边界条件清单
- [ ] 数据为空
- [ ] 数据量极大（1000+）
- [ ] 名称含特殊字符
- [ ] 权限被拒绝
- [ ] 网络不可用
- [ ] 进程被杀死后恢复
```

### 审核方案示例（基于上次三点菜单编辑功能的正确方案）

```markdown
## 审核方案：三点菜单→编辑元数据

### 审核范围
- 功能路径：长按歌曲 → 上下文菜单 → "编辑元数据" → 修改歌名/歌手 → 保存
- 涉及文件：LibraryScreen.kt, LibraryViewModel.kt, TagEditor.kt, MusicRepository.kt
- 测试环境：真机/MuMu模拟器，Android 9+

### 维度重点
- 功能：重点（新功能核心路径）
- UI：重点（对话框交互体验）
- 性能：一般（简单操作，无性能压力）
- 代码：重点（文件写入操作要严谨）
- 测试：一般（回归测试为主）

### 已知风险项
- MANAGE_EXTERNAL_STORAGE 权限不足导致编辑失败
- ADB 无法输入中文（模拟器限制）
- 歌曲文件写入时被系统锁定
- 编辑后 UI 不刷新

### 边界条件清单
- [x] 歌曲列表为空 → 没有可编辑对象
- [x] 歌名为空 → 如何显示？
- [x] 歌名/歌手超长 → 溢出处理
- [x] 包含特殊字符/emoji → 写入兼容性
- [x] 文件只读/受保护 → 写入失败处理
- [x] 编辑过程中退出 → 状态保持
```

---

# Phase 2：验证设计（战术层）

> **目的：** 对方案中的每个检查项，明确"怎么验"、"能发现什么问题"、"可能漏掉什么"。
>
> **核心原则：** 说不清怎么验证的项目，等于没审。不知道局限在哪里的验证，等于自欺欺人。

## 2.1 每个验证项必须包含的三要素

对于审核方案中的每个检查点，在设计时必须明确：

```
### [检查项名称]

验证方法：
- 具体步骤（第1步、第2步、第3步...）
- 使用的工具（截图/ADB/代码审查/视觉分析）
- 判断依据（什么算通过）

能发现的问题：
- 列举此步骤可能暴露的具体缺陷类型

验证局限：
- 此方法可能遗漏什么问题？
- 需要什么额外条件才能补全？

深度评估（三选一）：
- [表面] 肉眼可见，无需配置
- [中等] 需要特定操作或数据准备
- [深度] 需要专业工具/长时间运行/特殊环境
```

### 示例：正确的验证设计

```
### 检查项：编辑对话框预填值正确性

验证方法：
1. 长按列表中一曲目（已知：歌名="说了再见"，歌手="周杰伦"）
2. 点击"编辑元数据"
3. 截图对话框中标题和艺术家输入框
4. 比较预填值与实际元数据一致
5. 使用 vision_analyze 检查截图

能发现的问题：
- 预填值与实际不符（如显示文件名而非标题）
- 对话框标题显示截断/乱码
- 多行文本框高度不足
- 无法显示长文本

验证局限：
- 只能验证单个固定曲目，不能覆盖所有曲目格式
- 无法验证编辑后保存的功能（需要后续步骤）
- ASCII之外的文本（中文/日文/emoji）显示是否正确

深度评估：[中等]
```

## 2.2 验证深度自检清单

设计完验证方案后，必须回答以下问题：

```
□ 我有没有遗漏一个明显的问题路径？（如：点击保存但无网络）
□ 我的验证方法是唯一可行的方式吗？有没有更好的方法？
□ 我是否假设了"正常情况下"而忽略"异常情况"？
□ 如果我是用户，我会觉得这个验证够深入吗？
□ 我是否因为"不好测"而跳过了某个检查项？
```

**任何一项回答"是"或"不确定" → 必须补全验证设计。**

---

# Phase 3：执行验证

## 3.1 执行纪律

1. **严格按照验证设计方案执行，不得擅自增减步骤**
2. 每完成一步，标记 `[✅]` 或 `[❌]`
3. 遇到阻碍（如 ADB 不能输入中文），必须记录在案并寻找替代方案
4. 替代方案不可行时，必须在报告中注明"未验证"及原因
5. **不得以"这个很好测所以跳过"的理由省略任何步骤**

## 3.2 执行记录模板

```markdown
### [检查项名称]

预期结果：...
实际结果：[✅/❌]
证据：[截图/日志/代码]

通过条件：...
结论：通过 / 不通过（理由）
```

---

# Phase 4：报告与自省

## 4.1 评分报告模板

```markdown
### 审核结果总览

| 维度 | 权重 | 得分 | 扣分项 |
|:----|:---:|:----:|:-------|
| 功能完整性 | 30% | X.X | ... |
| UI/UX 品质 | 25% | X.X | ... |
| 性能稳定性 | 20% | X.X | ... |
| 代码质量 | 15% | X.X | ... |
| 测试覆盖 | 10% | X.X | ... |
| **总分** | **100%** | **X.XX** | |

### 未达标项清单

| 编号 | 类型 | 描述 | 严重度 | 修复建议 |
|:----|:----|:-----|:------|:---------|
| 1 | P1/P2 | ... | 高/中/低 | ... |

### 验证覆盖说明

- 总检查项：X 项
- 已验证：X 项
- 无法验证：X 项（原因：...）
- 验证覆盖率：X%
```

## 4.2 强制自省

审核报告的最后必须包含以下自省内容。**不得省略，不得敷衍。**

```markdown
## 审核自省

### 1. 有无跳过步骤？
[诚实回答：有/无。如果有，哪些？为什么？]

### 2. 有无假装深入实则敷衍？
[诚实回答：有/无。如果有，哪些检查项本可以更深入？]

### 3. 验证设计的局限是否真实反映在结论中？
[验证设计时标注的"局限"有没有在结论中体现？还是假装局限不存在？]

### 4. 如果用户亲自复检，最可能发现什么遗漏？
[站在用户视角，什么是我最容易忽略的？]

### 5. 本次审核给自己打分（不是项目分，是审核质量分）
- 严谨度：X/10
- 深度：X/10
- 诚实度：X/10
- 平均：X/10

### 6. 下次审核如何改进？
[具体改进点]
```

---

# 附录 A：审核 Checklist 速查

## Phase 0 通过条件
- [ ] 审核范围已定义
- [ ] 各维度满分标准已明确
- [ ] 目标分已设定
- [ ] 一票否决项已确认

## Phase 1 通过条件
- [ ] 审核方案已写完
- [ ] 维度重点已标注
- [ ] 已知风险已列出
- [ ] 边界条件已遍历

## Phase 2 通过条件
- [ ] 每个检查项有验证方法
- [ ] 每个检查项标注了能发现的问题
- [ ] 每个检查项标注了局限
- [ ] 深度自检清单已通过

## Phase 3 通过条件
- [ ] 所有验证项按方案执行完毕
- [ ] 未验证项有原因说明
- [ ] 所有失败项有证据

## Phase 4 通过条件
- [ ] 评分报告完整
- [ ] 自省部分已填写
- [ ] 改进方向已明确

---

> **最后提醒：** 这套规范的目的是防止敷衍，而不是增加负担。
> 每个阶段都有存在的理由——跳过任何一个，就等于打开了敷衍的门。
> 如果你觉得某个阶段"太麻烦"——**这就是最需要它的时候。**
