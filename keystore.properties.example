# Signing configuration for Muse release builds
# Copy this file to keystore.properties and fill in your values.
# keystore.properties is in .gitignore and will NOT be committed.

keystorePath=/path/to/your/keystore.jks
keystorePwd=your_keystore_password
keyAliasName=your_key_alias
keyPwd=your_key_password
