# Changelog

本项目遵循 Keep a Changelog 的结构，并在发布标签时由 GitHub 自动补充提交级变更说明。

## Unreleased

### Added

- 统一 `MediaClassifier` 与 OGG 有声书分类测试。
- 周期任务调度器、API 28 / 35 测试矩阵、严格 CI 与标签发布流程。
- 资源化 UI 副作用、悬浮歌词无障碍语义和项目维护文档。

### Changed

- 升级至 `compileSdk` / `targetSdk 36`、Kotlin 2.3、Room 2.8、Compose 2026.05、Media3 1.10 与 WorkManager 2.11 兼容线。
- 降低播放状态全局更新频率，并移除启动期非必要数据库与封面工作。
- 周期扫描与默认封面维护任务增加首轮延迟，并在升级后重排旧任务，避免安装 / 启动后立即抢占资源。
- 统一四标签导航，迷你播放器进入完整播放器。

### Fixed

- 修复重复通知按钮、关闭重复模式后仍重播、失效 Worker 链路和播放进度分类不一致。
- 修复数据库迁移、歌词与播放器仪器测试。
- 修复自动元数据写入失败仍更新数据库和继续执行歌词 / 封面副作用的问题；标签写入失败改为类型化错误，源文件读取支持物理路径兜底，写后验证改为内容比对，数据库提交失败或写入校验失败时会尝试回滚已写文件。
- 封面嵌入写入改为注入式 `TagEditor`，并增加源文件备份、写后内容校验和失败回滚；嵌入失败时仍保留本地封面缓存兜底。
- 清零代码级 Detekt 与 Android Lint 问题，移除硬编码 UI 文案和源码编码损坏。

### Removed

- 仓库中被跟踪的 Release APK 与未使用设置组件。
